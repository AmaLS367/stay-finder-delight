<div align="center">

# 🚀 Быстрый старт

**Руководство по быстрому старту для запуска StayFinder на вашей машине**

[← Домашняя страница документации](./README.md) • [Архитектура →](./architecture.md)

</div>

---

## 📋 Требования

Перед началом убедитесь, что у вас установлено следующее:

| Требование  | Версия    | Назначение         |
| ----------- | --------- | ------------------ |
| **Node.js** | 20+       | JavaScript runtime |
| **npm**     | 10+       | Менеджер пакетов   |
| **Git**     | Последняя | Контроль версий    |

### ✅ Проверка установки

```bash
# Проверить версию Node.js
node --version  # Должна быть v20.0.0 или выше

# Проверить версию npm
npm --version   # Должна быть 10.0.0 или выше

# Проверить Git
git --version
```

---

## 🔧 Установка

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/AmaLS367/stay-finder-delight.git
cd stay-finder-delight
```

### Шаг 2: Установка зависимостей

```bash
# Используя npm (рекомендуется)
npm ci

# Или используя npm install
npm install
```

> **💡 Совет:** `npm ci` рекомендуется, так как устанавливает точные версии из `package-lock.json`, обеспечивая согласованные сборки.

### Шаг 3: Запуск сервера разработки

```bash
npm run dev
```

Сервер разработки запустится на:

- **Локально:** `http://localhost:8080`
- **Сеть:** `http://[ваш-ip]:8080`

Вы должны увидеть вывод:

```
  VITE v5.4.19  ready in 500 ms

  ➜  Local:   http://localhost:8080/
  ➜  Network: http://192.168.x.x:8080/
```

---

## 🎯 Первые шаги

### 1. Изучение приложения

После запуска dev сервера:

1. **Главная страница** - Просмотрите рекомендуемые размещения и популярные направления
2. **Поиск** - Попробуйте поискать локацию (например, "Париж", "Токио")
3. **Детали объявления** - Кликните на любое объявление, чтобы увидеть полные детали
4. **Избранное** - Добавьте объявления в избранное (сохраняется в localStorage)
5. **Поездки** - Забронируйте размещение, чтобы увидеть его в поездках

### 2. Понимание структуры

Ознакомьтесь со структурой проекта:

```
src/
├── pages/          # Начните здесь - компоненты страниц
├── components/     # Переиспользуемые компоненты
├── hooks/         # Кастомные React hooks
├── lib/           # Утилиты
└── types/         # TypeScript типы
```

### 3. Внесение первого изменения

Попробуйте изменить главную страницу:

1. Откройте `src/pages/Home.tsx`
2. Измените заголовок hero с "Find your perfect stay" на ваше сообщение
3. Сохраните файл - Vite автоматически перезагрузит!

---

## 🛠️ Доступные скрипты

### Разработка

```bash
npm run dev          # Запустить сервер разработки (порт 8080)
npm run dev -- --port 3000  # Запустить на кастомном порту
```

### Сборка

```bash
npm run build        # Собрать для продакшена
npm run build:dev    # Собрать в режиме разработки
```

### Превью

```bash
npm run preview      # Превью продакшен сборки локально
```

### Качество кода

```bash
npm run lint         # Запустить ESLint
```

---

## 📁 Важные файлы

| Файл                 | Назначение                                       |
| -------------------- | ------------------------------------------------ |
| `src/App.tsx`        | Главный компонент приложения, настройка роутинга |
| `src/main.tsx`       | Точка входа приложения                           |
| `src/pages/Home.tsx` | Компонент главной страницы                       |
| `vite.config.ts`     | Конфигурация Vite                                |
| `tailwind.config.ts` | Конфигурация Tailwind CSS                        |
| `tsconfig.json`      | Конфигурация TypeScript                          |

---

## 🎨 Рабочий процесс разработки

### 1. Разработка компонентов

При создании нового компонента:

```typescript
// src/components/common/MyComponent.tsx
import type { MyComponentProps } from '@/types';

export function MyComponent({ prop }: MyComponentProps) {
  return <div>{prop}</div>;
}
```

### 2. Добавление новой страницы

1. Создайте компонент в `src/pages/`
2. Добавьте маршрут в `src/App.tsx`:

```typescript
const MyPage = lazy(() => import('./pages/MyPage'));

// В Routes:
<Route path="/mypage" element={<MyPage />} />
```

3. Добавьте ссылку навигации в `src/components/layout/Navbar.tsx`

### 3. Использование кастомных Hooks

```typescript
import { useWishlist } from "@/hooks/useWishlist";

function MyComponent() {
  const { wishlist, toggleWishlist } = useWishlist();
  // Используйте hook...
}
```

---

## 🔍 Советы по разработке

### Hot Module Replacement (HMR)

Vite предоставляет мгновенный HMR:

- **Изменения компонентов** - Обновляются мгновенно без перезагрузки страницы
- **Изменения стилей** - Применяются сразу
- **Сохранение состояния** - Состояние React сохраняется во время обновлений

### TypeScript

Проект использует строгий TypeScript:

- Все файлы должны иметь правильные типы
- Используйте алиас `@/` для импортов из `src/`
- Проверьте типы: `npx tsc --noEmit`

### Стилизация

- Используйте утилитарные классы **Tailwind CSS**
- Кастомные стили в `src/index.css`
- Специфичные для компонентов стили с Tailwind

### Отладка

1. **React DevTools** - Установите расширение браузера
2. **Console Logs** - Используйте `console.log` (удаляется в продакшене)
3. **Vite DevTools** - Встроенный overlay ошибок

---

## 🐛 Решение проблем

### Порт уже используется

```bash
# Убить процесс на порту 8080 (Windows)
netstat -ano | findstr :8080
taskkill /PID <PID> /F

# Или использовать другой порт
npm run dev -- --port 3000
```

### Модуль не найден

```bash
# Очистить node_modules и переустановить
rm -rf node_modules package-lock.json
npm install
```

### Ошибки TypeScript

```bash
# Проверить ошибки типов
npx tsc --noEmit

# Перезапустить TypeScript сервер в вашей IDE
```

### Ошибки сборки

```bash
# Очистить кеш Vite
rm -rf node_modules/.vite
npm run build
```

---

## 📚 Следующие шаги

Теперь, когда вы настроены:

1. 📖 Прочитайте [Руководство по архитектуре](./architecture.md) для понимания системы
2. 🧩 Изучите [Компоненты](./components.md), чтобы увидеть что доступно
3. 🎣 Узнайте о [Кастомных Hooks](./hooks.md)
4. 📊 Поймите [Структуры данных](./api-data.md)

---

## 💡 Общие задачи

### Добавление новой зависимости

```bash
npm install package-name
npm install -D dev-package-name  # Для dev зависимостей
```

### Обновление зависимостей

```bash
npm update
npm outdated  # Проверить обновления
```

### Запуск проверки типов

```bash
npx tsc --noEmit
```

---

<div align="center">

**Готовы строить?** Изучите [Руководство по архитектуре](./architecture.md) →

</div>
