<div align="center">

# 🏗️ Архитектура

**Дизайн системы, структура и архитектурные паттерны StayFinder**

[← Домашняя страница документации](./README.md) • [Компоненты →](./components.md)

</div>

---

## 📐 Обзор системы

StayFinder следует **компонентной архитектуре только фронтенда** с четким разделением ответственности. Приложение построено на React с TypeScript, используя современные паттерны и лучшие практики.

### Основные принципы

- 🧩 **Компонентная композиция** - Маленькие, переиспользуемые компоненты
- 📦 **Разделение ответственности** - Четкие границы между слоями
- 🔒 **Типобезопасность** - Полное покрытие TypeScript
- ⚡ **Производительность** - Оптимизированный рендеринг и ленивая загрузка
- 💾 **Локальное хранение** - localStorage для данных пользователя

---

## 🗂️ Структура проекта

```
stay-finder-delight/
│
├── 📁 public/                    # Статические ресурсы
│   ├── favicon.svg              # Иконка приложения
│   ├── site.webmanifest        # PWA манифест
│   └── robots.txt               # SEO конфигурация
│
├── 📁 src/
│   ├── 📁 components/           # React компоненты
│   │   ├── 📁 common/           # Бизнес-компоненты
│   │   │   ├── ListingCard.tsx  # Карточка объявления
│   │   │   └── SearchForm.tsx   # Форма поиска
│   │   │
│   │   ├── 📁 layout/           # Компоненты макета
│   │   │   ├── Layout.tsx       # Обертка макета
│   │   │   ├── Navbar.tsx       # Панель навигации
│   │   │   └── Footer.tsx       # Футер
│   │   │
│   │   ├── NavLink.tsx          # Кастомная ссылка навигации
│   │   │
│   │   └── 📁 ui/               # shadcn/ui примитивы (50+)
│   │       ├── button.tsx
│   │       ├── card.tsx
│   │       └── ...              # Еще UI компоненты
│   │
│   ├── 📁 data/                 # Статические данные
│   │   └── listings.json        # Объявления о размещении
│   │
│   ├── 📁 hooks/                # Кастомные React hooks
│   │   ├── useWishlist.ts       # Управление избранным
│   │   ├── useBookings.ts       # Управление бронированиями
│   │   ├── useLocalStorage.ts   # Обертка localStorage
│   │   └── use-mobile.tsx       # Определение мобильного устройства
│   │
│   ├── 📁 lib/                  # Утилиты и помощники
│   │   ├── constants.ts         # Константы приложения
│   │   ├── formatters.ts        # Функции форматирования
│   │   ├── dateUtils.ts         # Утилиты для дат
│   │   ├── queryParams.ts       # Обработка URL запросов
│   │   └── storage.ts           # Помощники для хранилища
│   │
│   ├── 📁 pages/                # Компоненты страниц
│   │   ├── Home.tsx             # Главная страница
│   │   ├── Search.tsx           # Результаты поиска
│   │   ├── Listing.tsx          # Детали объявления
│   │   ├── Trips.tsx            # Поездки пользователя
│   │   └── Wishlist.tsx         # Избранное пользователя
│   │
│   ├── 📁 types/                # Определения TypeScript
│   │   └── index.ts             # Все определения типов
│   │
│   ├── App.tsx                  # Главный компонент приложения
│   ├── main.tsx                 # Точка входа
│   └── index.css                # Глобальные стили
│
├── 📁 docs/                     # Документация
├── 📁 .github/workflows/        # CI/CD
│   └── deploy-pages.yml         # GitHub Actions
│
└── Файлы конфигурации
    ├── vite.config.ts          # Конфигурация Vite
    ├── tailwind.config.ts      # Конфигурация Tailwind
    └── tsconfig.json            # Конфигурация TypeScript
```

---

## 🏛️ Архитектурные слои

```mermaid
graph TB
    subgraph "Слой представления"
        A[Страницы] --> B[Компоненты макета]
        A --> C[Общие компоненты]
        C --> D[UI компоненты]
    end

    subgraph "Слой бизнес-логики"
        E[Кастомные Hooks] --> F[Управление состоянием]
        E --> G[Обработка данных]
    end

    subgraph "Слой данных"
        H[Статический JSON] --> I[Данные объявлений]
        J[LocalStorage] --> K[Данные пользователя]
    end

    subgraph "Инфраструктурный слой"
        L[Роутинг] --> A
        M[Инструменты сборки] --> N[Vite]
        O[Стилизация] --> P[Tailwind]
    end

    A --> E
    E --> H
    E --> J
    B --> E
```

---

## 🔄 Поток данных

```mermaid
sequenceDiagram
    participant User as Пользователь
    participant Page as Страница
    participant Hook as Hook
    participant Storage as Хранилище
    participant Data as Данные

    User->>Page: Взаимодействует с UI
    Page->>Hook: Вызывает функцию hook
    Hook->>Storage: Читает/Записывает данные
    Storage->>Hook: Возвращает данные
    Hook->>Page: Обновляет состояние
    Page->>User: Обновляет UI

    Note over Page,Data: Статический поток данных
    Page->>Data: Читает listings.json
    Data->>Page: Возвращает объявления
```

---

## 🧩 Компонентная архитектура

### Иерархия компонентов

```mermaid
graph TD
    A[App] --> B[HashRouter]
    B --> C[Routes]
    C --> D[Главная страница]
    C --> E[Страница поиска]
    C --> F[Страница объявления]
    C --> G[Страница поездок]
    C --> H[Страница избранного]

    D --> I[Layout]
    E --> I
    F --> I
    G --> I
    H --> I

    I --> J[Navbar]
    I --> K[Footer]
    I --> L[Содержимое страницы]

    L --> M[SearchForm]
    L --> N[ListingCard]
    L --> O[UI компоненты]

    M --> O
    N --> O
```

### Категории компонентов

| Категория  | Назначение                           | Примеры                             |
| ---------- | ------------------------------------ | ----------------------------------- |
| **Pages**  | Компоненты маршрутов верхнего уровня | `Home.tsx`, `Search.tsx`            |
| **Layout** | Структура и навигация                | `Layout.tsx`, `Navbar.tsx`          |
| **Common** | Переиспользуемые бизнес-компоненты   | `ListingCard.tsx`, `SearchForm.tsx` |
| **UI**     | Низкоуровневые примитивы             | `Button`, `Card`, `Dialog`          |

---

## 🔀 Архитектура роутинга

StayFinder использует **HashRouter** для клиентского роутинга:

```mermaid
graph LR
    A[HashRouter] --> B[Routes]
    B --> C["/ - Главная"]
    B --> D["/search - Поиск"]
    B --> E["/listing/:id - Детали"]
    B --> F["/trips - Поездки"]
    B --> G["/wishlist - Избранное"]
    B --> H["* - 404"]

    style C fill:#e1f5ff
    style D fill:#e1f5ff
    style E fill:#e1f5ff
    style F fill:#e1f5ff
    style G fill:#e1f5ff
    style H fill:#ffe1e1
```

### Почему HashRouter?

- ✅ Работает со статическим хостингом (GitHub Pages)
- ✅ Не требуется конфигурация сервера
- ✅ Избегает ошибок 404 при обновлении
- ✅ Глубокие ссылки работают из коробки

---

## 💾 Управление состоянием

### Архитектура состояния

```mermaid
graph TB
    subgraph "Состояние компонента"
        A[useState] --> B[Локальное состояние компонента]
    end

    subgraph "Постоянное состояние"
        C[useLocalStorage] --> D[LocalStorage]
        E[useWishlist] --> C
        F[useBookings] --> C
    end

    subgraph "URL состояние"
        G[useSearchParams] --> H[Параметры запроса]
    end

    I[Страницы] --> A
    I --> E
    I --> F
    I --> G
```

### Стратегия управления состоянием

| Тип состояния             | Решение           | Использование                      |
| ------------------------- | ----------------- | ---------------------------------- |
| **Состояние компонента**  | `useState`        | UI состояние, поля форм            |
| **Постоянное состояние**  | `useLocalStorage` | Избранное, бронирования            |
| **URL состояние**         | `useSearchParams` | Фильтры поиска, пагинация          |
| **Производное состояние** | `useMemo`         | Отфильтрованные списки, вычисления |

---

## 🎣 Архитектура Hooks

### Поток кастомных Hooks

```mermaid
graph TD
    A[useLocalStorage] --> B[Базовый Hook]
    B --> C[useWishlist]
    B --> D[useBookings]

    C --> E[Операции избранного]
    D --> F[Операции бронирований]

    E --> G[Добавить/Удалить/Переключить]
    F --> H[Создать/Отменить/Фильтровать]

    G --> I[LocalStorage]
    H --> I
```

### Зависимости Hooks

```mermaid
graph LR
    A[useLocalStorage] --> B[Основное хранилище]
    C[useWishlist] --> A
    D[useBookings] --> A
    D --> E[dateUtils]

    F[Страницы] --> C
    F --> D
```

---

## 📊 Паттерны потока данных

### Поток поиска

```mermaid
sequenceDiagram
    participant User as Пользователь
    participant SearchForm as Форма поиска
    participant SearchPage as Страница поиска
    participant QueryParams as Параметры запроса
    participant Listings as Объявления

    User->>SearchForm: Вводит критерии поиска
    SearchForm->>QueryParams: Строит URL
    QueryParams->>SearchPage: Обновляет маршрут
    SearchPage->>Listings: Фильтрует данные
    Listings->>SearchPage: Возвращает результаты
    SearchPage->>User: Отображает результаты
```

### Поток бронирования

```mermaid
sequenceDiagram
    participant User as Пользователь
    participant ListingPage as Страница объявления
    participant useBookings as useBookings
    participant LocalStorage as LocalStorage
    participant TripsPage as Страница поездок

    User->>ListingPage: Нажимает "Забронировать"
    ListingPage->>useBookings: createBooking()
    useBookings->>LocalStorage: Сохраняет бронирование
    LocalStorage->>useBookings: Подтверждает сохранение
    useBookings->>ListingPage: Возвращает бронирование
    ListingPage->>User: Показывает подтверждение
    User->>TripsPage: Просматривает поездки
    TripsPage->>useBookings: Читает бронирования
    useBookings->>LocalStorage: Получает данные
    LocalStorage->>TripsPage: Возвращает бронирования
```

---

## 🎨 Архитектура стилизации

### Структура Tailwind CSS

```mermaid
graph TB
    A[Tailwind CSS] --> B[Утилитарные классы]
    B --> C[Компоненты]
    C --> D[Страницы]

    E[Кастомная конфигурация] --> A
    F[Переменные темы] --> A
    G[Кастомные утилиты] --> A
```

### Подход к стилизации

- **Utility-First** - Утилитарные классы Tailwind
- **Варианты компонентов** - Использование `class-variance-authority`
- **Система тем** - CSS переменные для темизации
- **Адаптивный дизайн** - Подход mobile-first

---

## ⚡ Оптимизация производительности

### Разделение кода

```mermaid
graph LR
    A[App.tsx] --> B[Ленивая загрузка]
    B --> C[Home]
    B --> D[Search]
    B --> E[Listing]
    B --> F[Trips]
    B --> G[Wishlist]

    H[Изменение маршрута] --> I[Загрузка компонента]
    I --> J[Рендер]
```

### Стратегии оптимизации

| Техника                     | Реализация               | Преимущество                      |
| --------------------------- | ------------------------ | --------------------------------- |
| **Ленивая загрузка**        | `React.lazy()`           | Меньший начальный бандл           |
| **Разделение кода**         | По маршрутам             | Загрузка по требованию            |
| **Мemoизация**              | `useMemo`, `useCallback` | Предотвращение повторных рендеров |
| **Оптимизация изображений** | Ленивая загрузка         | Быстрая загрузка страницы         |

---

## 🔐 Типобезопасность

### Архитектура TypeScript

```mermaid
graph TB
    A[Определения типов] --> B[Интерфейсы]
    A --> C[Типы]
    A --> D[Перечисления]

    B --> E[Listing]
    B --> F[Booking]
    B --> G[Host]

    E --> H[Компоненты]
    F --> H
    G --> H

    H --> I[Типобезопасность]
```

### Покрытие типами

- ✅ **100% покрытие типами** - Все файлы типизированы
- ✅ **Строгий режим** - TypeScript strict включен
- ✅ **Вывод типов** - Использует вывод типов TypeScript
- ✅ **Обобщенные типы** - Переиспользуемые паттерны типов

---

## 🚀 Сборка и развертывание

### Процесс сборки

```mermaid
graph LR
    A[Исходный код] --> B[Vite]
    B --> C[Компиляция TypeScript]
    C --> D[Бандлинг]
    D --> E[Оптимизация]
    E --> F[Статические ресурсы]
    F --> G[dist/]

    H[GitHub Actions] --> B
    G --> I[GitHub Pages]
```

### Поток развертывания

```mermaid
sequenceDiagram
    participant Dev as Разработчик
    participant GitHub as GitHub
    participant Actions as Actions
    participant Pages as Pages

    Dev->>GitHub: Push в main
    GitHub->>Actions: Запускает workflow
    Actions->>Actions: Устанавливает зависимости
    Actions->>Actions: Собирает проект
    Actions->>Actions: Загружает артефакт
    Actions->>Pages: Развертывает
    Pages->>Dev: Сайт в сети
```

---

## 📦 Управление зависимостями

### Основные зависимости

```mermaid
graph TB
    A[React] --> B[Основной фреймворк]
    C[React Router] --> D[Роутинг]
    E[shadcn/ui] --> F[UI компоненты]
    G[Tailwind] --> H[Стилизация]
    I[TypeScript] --> J[Типобезопасность]

    B --> K[Приложение]
    D --> K
    F --> K
    H --> K
    J --> K
```

---

## 🎯 Паттерны проектирования

### Используемые паттерны

| Паттерн                     | Реализация                            | Назначение              |
| --------------------------- | ------------------------------------- | ----------------------- |
| **Компонентная композиция** | Маленькие, сфокусированные компоненты | Переиспользование       |
| **Кастомные Hooks**         | Извлечение бизнес-логики              | Организация кода        |
| **Higher-Order Components** | Обертка макета                        | Переиспользование кода  |
| **Render Props**            | Гибкий API компонентов                | Гибкость                |
| **Управляемые компоненты**  | Поля форм                             | Предсказуемое состояние |

---

## 🔄 Жизненный цикл и обновления

### Жизненный цикл компонента

```mermaid
graph TD
    A[Монтирование компонента] --> B[useEffect]
    B --> C[Получение данных]
    C --> D[Обновление состояния]
    D --> E[Перерендер]
    E --> F[Взаимодействие пользователя]
    F --> G[Изменение состояния]
    G --> E
    E --> H[Размонтирование компонента]
    H --> I[Очистка]
```

---

<div align="center">

**Следующее:** Изучите [Компоненты](./components.md) →

</div>
